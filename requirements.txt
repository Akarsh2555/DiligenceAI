fastapi==0.115.0
uvicorn[standard]==0.30.6
# NOTE: this machine has langchain-core v1 (1.4.5) installed alongside the
# v0.3 meta packages below. That mix is what the app actually runs on, so we
# pin langchain-core + langsmith to the v1 versions to stop `pip install -r`
# from downgrading them (which breaks langgraph-prebuilt / langchain-anthropic).
# pip will print harmless "dependency conflict" warnings — they are expected.
langchain==0.3.7
langchain-core
langchain-google-genai==2.0.7
langchain-community==0.3.7
langgraph==0.2.53
langsmith
pypdf==5.1.0
python-docx==1.1.2
openpyxl==3.1.5
supabase==2.11.0
python-multipart==0.0.12
pydantic-settings==2.6.1
pydantic==2.10.2
httpx==0.27.2
sse-starlette==2.1.3
python-dotenv==1.0.1
aiofiles==24.1.0
gunicorn==22.0.0
matplotlib==3.10.9
# Vector store is now Supabase pgvector (supabase/pgvector_migration.sql).
# Removed: chromadb, langchain-chroma, langchain-huggingface, sentence-transformers
# — they pulled torch (~500MB) + onnxruntime and OOM-crashed the 512MB host.
